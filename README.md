this application require `sshpass` package to run connection using password

Installation :
`wget -O - https://raw.githubusercontent.com/godamri/ssh-connect/main/install_script.sh | bash`

Using Command :
`connect ssh`